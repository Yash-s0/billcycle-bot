import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('home screen golden placeholder', (WidgetTester tester) async {
    // Golden setup requires assets + deterministic fonts in CI.
    // Keep this test skipped by default until design assets are locked.
  }, skip: true);
}
